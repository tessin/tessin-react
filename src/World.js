import React from "react";

export default function() {
  return (
    <div>
      <h1>A deferred component</h1>
      <p>
        We employ two tricks to solve this particular code splitting issue. The
        first trick is to use a static route configuration. It's very important
        to do so.
      </p>
      <p>To read about the other trick, click the /magic/ link.</p>
    </div>
  );
}
